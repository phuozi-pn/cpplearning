# W01 周计划（2026.5.13 - 2026.5.19）

> **本周主题**：环境搭建 + C++11 复习 + 第一个 socket
> **本周目标**：环境完整 + C++ 核心特性熟练 + 阻塞/多进程 echo + LeetCode 13 题

---

## 本周验收清单（周日填）

- [ ] WSL2/虚拟机 + 工具链完整
- [ ] GitHub 仓库 `cpp-learning` 建立，至少 7 个 commit
- [ ] C++ 智能指针 / move / lambda / thread 复习完成
- [ ] 写完 3 个 demo：`MyString` / 简化版 `unique_ptr` / 生产者消费者
- [ ] 阻塞 TCP echo server + 多进程版本
- [ ] LeetCode 累计 13 题
- [ ] 写 1 篇周博客
- [ ] 墨墨背单词 / 不背单词 App 开始打卡

---

## 周三 5.13 今晚（2-3h）— 环境攻坚

### 必做（睡前必须完成）

```
□ 1. WSL2 / 虚拟机
   方案A: PowerShell 管理员: wsl --install -d Ubuntu-22.04
   方案B: VirtualBox / VMware + Ubuntu 镜像

□ 2. 工具链安装（Ubuntu 终端内）
   sudo apt update
   sudo apt install -y build-essential cmake git gdb vim curl wget

□ 3. 验证版本
   g++ --version       # 期望 >= 11
   cmake --version     # 期望 >= 3.20
   git --version

□ 4. VSCode
   - 装 VSCode
   - 装插件：WSL、C/C++、CMake Tools、GitLens

□ 5. Git 配置
   git config --global user.name "你的英文名"
   git config --global user.email "你的邮箱"

□ 6. GitHub
   - 注册 / 登录 GitHub
   - 建公开仓库 cpp-learning
   - 本地 git clone

□ 7. 第一行代码
   - 在仓库内建目录 week01/
   - 写 hello.cpp:
       #include <iostream>
       int main() { std::cout << "Hello, Big Tech!\n"; return 0; }
   - 编译: g++ hello.cpp -o hello && ./hello
   - git add . && git commit -m "first commit" && git push

□ 8. LeetCode
   - 注册 leetcode.cn
   - 刷"两数之和"（C++）
   - 提交 AC

□ 9. 考研保底 App
   - 装"墨墨背单词" / "不背单词"
   - 选"考研 5500 词"词库
   - 今晚不背，明天开始
```

⏰ **底线**：第 1-7 步必须做完，第 8-9 步可以推到明天。

---

## 周四 5.14（5h）— 智能指针

### 主线 C++（3h）

**视频（1.5h）**：
- B 站搜「侯捷 C++11」，看智能指针章节
- 1.5 倍速，重点：unique_ptr / shared_ptr / weak_ptr
- 看完 cppreference 智能指针文档（英文，先适应）

**编码（1.5h）**：
- 写 demo: 用智能指针管理对象（30 行）
- **手写简化版 `unique_ptr<T>`**（50-80 行）
  - 构造 / 析构
  - `operator->` / `operator*`
  - 移动构造 / 移动赋值
  - 禁用拷贝（`= delete`）
- 测试: `MyUniquePtr<int> p(new int(42)); std::cout << *p;`

### 算法（1h）— 2 题

- [ ] [回文数](https://leetcode.cn/problems/palindrome-number/)
- [ ] [罗马数字转整数](https://leetcode.cn/problems/roman-to-integer/)

### 弹性（1h）

- 30min 墨墨单词（50-80 词）
- 30min 复盘 + commit + push

---

## 周五 5.15（5h）— move 语义

### 主线 C++（3h）

**视频（1h）**：
- 侯捷视频：右值引用 + move 语义 + 完美转发
- 重点：左值/右值的判定，`std::move` 实际做了什么

**编码（2h）**：
- 写 `MyString` 类（150-200 行）
  - 构造函数 `MyString(const char*)`
  - **拷贝构造** + **拷贝赋值**
  - **移动构造** + **移动赋值**
  - 析构（删除堆内存）
  - 用 `std::vector<MyString>` 测试，加 `std::cout` 验证哪个构造被调用

### 算法（1h）

- [ ] [最长公共前缀](https://leetcode.cn/problems/longest-common-prefix/)
- [ ] [有效的括号](https://leetcode.cn/problems/valid-parentheses/)

### 弹性（1h）

- 30min 墨墨单词
- 30min 复盘 + commit

---

## 周六 5.16（6h，周末多 1h）

### 上午 C++（3h）— lambda + std::function

- 视频（1h）：侯捷 lambda 章节 + `std::function` / `std::bind`
- 编码（2h）：写一个"事件分发器"
  - `class EventDispatcher`
  - `void subscribe(string event, function<void()> cb)`
  - `void publish(string event)`
  - 用 lambda 注册 3-5 个回调，测试

### 下午 C++（2h）— 模板 + auto

- 视频（30min）：函数模板 + 类模板基础 + auto / decltype
- 编码（1.5h）：实现简化版 `MyVector<T>`
  - 模板类
  - push_back（含扩容逻辑）
  - operator[]
  - size / capacity
  - 析构

### 算法（1h）

- [ ] [合并两个有序链表](https://leetcode.cn/problems/merge-two-sorted-lists/)
- [ ] [删除有序数组中的重复项](https://leetcode.cn/problems/remove-duplicates-from-sorted-array/)

---

## 周日 5.17（6h）— 第一个 socket ⭐

### 主线（4h）

**视频（1.5h）**：
- B 站搜「牛客 Linux 高并发服务器开发」
- 看前 3 集（TCP/IP 复习 + socket API 概览）

**编码（2.5h）**：
- 写阻塞 TCP echo server（80-100 行）
  - `socket` / `bind` / `listen` / `accept`
  - `recv` / `send` 循环
  - 错误处理（`perror`）
- 写 client（60-80 行）
  - `socket` / `connect` / `send` / `recv`
- 测试：一个终端跑 server，另一个跑 client
- 提交到 GitHub 的 `week01/echo/`

### 算法（1h）

- [ ] [移除元素](https://leetcode.cn/problems/remove-element/)
- [ ] [找出字符串中第一个匹配项的下标](https://leetcode.cn/problems/find-the-index-of-the-first-occurrence-in-a-string/)

### 周博客 + 复盘（1h）

- 写博客《我的大厂之路 Week 1：从环境到第一个 socket》
- 推荐发到 [掘金](https://juejin.cn)
- 把 GitHub README 更新一下，写清楚每个 demo 干啥

---

## 周一 5.18（5h）— 多线程

### 主线（3h）

**视频（1h）**：
- 侯捷《C++11》并发章节
- `std::thread` / `mutex` / `condition_variable`

**编码（2h）**：
- 写生产者-消费者模型（200 行）
  - 线程安全队列 `ThreadSafeQueue<T>`
  - 2 个生产者线程 + 3 个消费者线程
  - 用 `mutex` + `condition_variable`
  - 用 `std::atomic<bool>` 控制停止

### 算法（1h）

- [ ] [加一](https://leetcode.cn/problems/plus-one/)
- [ ] [二进制求和](https://leetcode.cn/problems/add-binary/)

### 弹性（1h）

- 30min 墨墨单词
- 30min 复盘 + commit

---

## 周二 5.19（5h）— 多进程 socket

### 主线（3h）

**视频（1h）**：
- 牛客 Linux 网络编程：fork / 进程通信 / 多进程服务器章节

**编码（2h）**：
- 改造周日的 echo server，变成多进程版
  - 主进程 `accept`
  - 每个新连接 `fork` 一个子进程处理
  - 用 `signal(SIGCHLD, SIG_IGN)` 或 `waitpid` 避免僵尸进程
- 用 2-3 个客户端同时连接，验证并发

### 算法（1h）

- [ ] [爬楼梯](https://leetcode.cn/problems/climbing-stairs/)（DP 入门）
- [ ] [删除排序链表中的重复元素](https://leetcode.cn/problems/remove-duplicates-from-sorted-list/)

### 周复盘（1h）— 必填

填写 [06-周复盘模板.md](./06-周复盘模板.md) 的 W01 部分，包括：
- 完成情况
- 未完成项（挪到下周或砍掉）
- 学到的核心知识点（自己讲一遍）
- 遗留问题
- 下周（W2）重点预告

---

## 本周文件目录建议

```
cpp-learning/
├── README.md
├── week01/
│   ├── hello.cpp
│   ├── smart_ptr/
│   │   ├── my_unique_ptr.cpp
│   │   └── smart_ptr_demo.cpp
│   ├── move/
│   │   └── my_string.cpp
│   ├── lambda/
│   │   └── event_dispatcher.cpp
│   ├── template/
│   │   └── my_vector.cpp
│   ├── thread/
│   │   └── producer_consumer.cpp
│   └── echo/
│       ├── echo_server.cpp
│       ├── echo_server_fork.cpp
│       └── echo_client.cpp
└── progress.md
```

---

## 本周防翻车清单

| 风险 | 应对 |
|---|---|
| WSL 装不上 | 改虚拟机，不要在环境上死磕 > 2h |
| 视频看太久不动手 | **看到关键概念立刻暂停写代码** |
| 算法卡题超 30 分钟 | 立刻看题解，刷题是学模式不是比智商 |
| 想"再多复习一下再开始" | 砍掉这个念头，**今晚必须开始** |
| 单词背着背着想刷数学 | 别！本月只背单词 |
| commit 太碎 / 太大 | 一天 1-3 个 commit 即可 |

---

## 今晚立刻执行的 5 件事（再次提醒）

```
1. □ 装 WSL2 / 虚拟机 + 工具链
2. □ VSCode + Remote 插件
3. □ GitHub 建仓库 cpp-learning
4. □ 写 hello.cpp + commit + push
5. □ LeetCode 注册 + 第 1 题 AC
```

**完成这 5 件事才能睡觉。**
