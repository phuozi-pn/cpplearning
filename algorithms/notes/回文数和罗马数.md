回文数： 
暴力解很容易：转换成字符串，然后双指针，但缺点是新增额外开销。
例外注意细节：string s ,小写，对数字转换 std:: to_string(x)

巧秒解法：关键在于 如何求回文数： revertedNum=revertedNum*10+x%10

罗马数：知识点在于hash ,创建一个unordered_map<char,int > map={{'I',1}}

    同时，分为两种情况，简化为两个排列之间的大小，这一点很巧妙，这是思路上的
    同时它的边界处理和情况分类也值得学习，分的太好了
    if(i+1<n && map[s[i]]< map[s[i+1]]){
                sum=sum-map[s[i]];
    }
    else{
                sum=sum+map[s[i]];
    }