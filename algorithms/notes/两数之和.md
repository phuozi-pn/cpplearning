暴力解法，两层循环，以外层i为标准，向后遍历寻找。
hash解法：一层循环，通过undered_map<int,int>实现hash,在已遍历数组中寻找，即向i之前寻找，
思考：一般hash是不是都有这种特点。
关键难点：关键卡点
一开始想"先把所有数存进 hash，再遍历找另一半"，但意识到： 如果先全部存入，无法处理重复[3,3]。 所以正确做法是"先查再存"。

"在数组里找/统计/判重"，就先问自己：能用 hash 吗